# sdl-game-dev

## SDL Create App
### using command line to build and run:
    cd ./p1-sdl-create-app
    mkdir build<br/>
    cd ./build<br/>
    cmake ..<br/>
    cmake --build .<br/>
    # run app<br/>
    ./SdlCreateApp<br/>
